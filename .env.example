# Reference FastAPI/PostgreSQL implementation only; live Firestore pilot uses ADC and tools/.
# Configuration template, not credentials. Never commit a real .env file.
DATABASE_URL=postgresql://signrush_runtime:REPLACE_ME@HOST/signrush?sslmode=verify-full
FIREBASE_PROJECT_ID=signrush-login
SIGNRUSH_PILOT_UIDS=REPLACE_WITH_INVITED_FIREBASE_UIDS
# Use the cloud runtime's attached service account (ADC), not downloaded keys.
